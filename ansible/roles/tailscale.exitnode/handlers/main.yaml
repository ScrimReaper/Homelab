---
- name: Restart systemd-resolved
  ansible.builtin.systemd:
    name: systemd-resolved
    state: restarted

- name: Restart WireGuard tunnel
  ansible.builtin.systemd:
    name: wg-quick@{{ proton_interface }}
    state: restarted
