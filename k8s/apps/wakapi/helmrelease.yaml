apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wakapi
  namespace: wakapi
spec:
  interval: 30m
  chart:
    spec:
      chart: wakapi
      version: 1.0.30
      sourceRef:
        kind: HelmRepository
        name: wakapi
        namespace: flux-system
      interval: 24h
